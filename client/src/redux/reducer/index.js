const initialState = {
  videogames: [],
  videogameDetail: [],
  geners:[],
  platforms:[]
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    
  }
};

export default rootReducer;